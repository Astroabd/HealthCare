server:
  port: 8888
spring:
  application:
    name: configserver
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: git@github.com:Astroabd/ConfigHealth.git
          clone-on-start: true
          default-label: master